# Version-Bound Approval Policy & Multi-Level Workflows

- Approvals bind strictly to `resource_id + version_id + hash`. Modifying an approved report creates a new draft version requiring re-approval.
