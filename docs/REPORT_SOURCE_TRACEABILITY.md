# Report Source Traceability Specification

## Strict Rule: Every Paragraph Has a Source
- Every substantive paragraph maps to a `CaseFact`, `EvidenceFile`, `StatementAnswer`, or `TimelineEvent`.
- Paragraphs without verifiable sources are flagged as `UNSUPPORTED_CLAIM`.
